% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_pci.R
\name{fit_pci}
\alias{fit_pci}
\title{Provider Tests, Empirical-Null Calibration, and Confidence Intervals}
\usage{
fit_pci(
  z = NULL,
  estimate = NULL,
  se = NULL,
  observed = NULL,
  expected = NULL,
  p_value = NULL,
  direction = NULL,
  statistic = c("z", "wald", "log_ratio", "poisson_midp", "poisson_exact",
    "poisson_score", "poibin", "binary_score", "resampling", "p_value"),
  calibration = c("theoretical", "fixed", "overall", "groupwise", "individualized",
    "correlated", "cre", "moment"),
  reference = NULL,
  transform = c("identity", "log", "logit"),
  se_scale = c("original", "transformed"),
  df = Inf,
  size = NULL,
  covariates = NULL,
  second_moment = NULL,
  null_mean = 0,
  null_sd = 1,
  psi = c("bisquare", "huber"),
  groups = NULL,
  n_groups = 4,
  grouping = c("quantile", "rank"),
  common_mean = NULL,
  fit_mask = NULL,
  probabilities = NULL,
  eta = NULL,
  re_mean = NULL,
  re_var = NULL,
  alpha = 0.05,
  alternative = c("two.sided", "greater", "less"),
  ci = c("auto", "none", "wald", "poisson", "invert"),
  mid_p = TRUE,
  zero_method = c("score", "exclude"),
  id = NULL,
  na_action = c("fail", "omit"),
  control = list()
)
}
\arguments{
\item{z}{Precomputed provider-level normal scores.}

\item{estimate, se}{Estimates and standard errors. For \code{statistic="log_ratio"},
\code{se} is on the log-ratio scale by default.}

\item{observed, expected}{Observed integer counts and nonnegative expected
counts. Zero expected counts do not define an observed/expected ratio.}

\item{p_value, direction}{Supplied two-sided p-values and signed directions
(negative below the reference, positive above).}

\item{statistic}{One of \code{z}, \code{wald}, \code{log_ratio}, \code{poisson_midp},
\code{poisson_exact}, \code{poisson_score}, \code{poibin}, \code{binary_score}, \code{resampling},
or \code{p_value}. \code{poibin_exact} is an alias for \code{poibin}.}

\item{calibration}{One of \code{theoretical}, \code{fixed}, \code{overall}, \code{groupwise},
\code{individualized}, \code{correlated}, \code{cre}, or \code{moment}.}

\item{reference}{Reference on the original estimate scale. The defaults are
zero for identity, one for log, and 0.5 for logit. A Poisson reference is
a nonnegative ratio multiplying \code{expected}, default one. For binary
\code{eta} input this is an additive log-odds effect, default zero; when
supplying \code{probabilities}, encode the null in those probabilities.}

\item{transform}{Working transformation for Wald inference: \code{identity},
\code{log}, or \code{logit}. Numeric reference values are transformed automatically.}

\item{se_scale}{Whether a Wald SE is on the \code{original} or \code{transformed} scale.}

\item{df}{Wald reference degrees of freedom. Infinite means normal; finite
t statistics are converted to normal quantiles before calibration.}

\item{size}{Positive provider sizes for grouping or structured nulls. For
Poisson moment calibration, this is the expected-count vector and defaults
to \code{expected} when \code{statistic="poisson_score"}.}

\item{covariates}{Provider-level numeric covariates, with one row per
provider; used by \code{correlated} and \code{moment} calibration.}

\item{second_moment}{For Poisson moment calibration, the sum of squared
observation-level expected counts within each provider. This is not the
square of the provider's total expected count. Requires Pearson scores.}

\item{null_mean, null_sd}{Supplied mean and SD for \code{calibration="fixed"}.
Each may be a scalar or one value per provider. For example,
\code{null_sd=1.81} specifies the SD of the score distribution; it does not
set the outcome reference or supply a variance component.}

\item{psi}{Robust influence function: \code{bisquare} or \code{huber}.}

\item{groups}{Optional explicit group labels, one per provider.}

\item{n_groups}{Requested number of size groups for \code{groupwise} calibration;
one places all providers in a single group. Ignored with explicit \code{groups}.}

\item{grouping}{\code{quantile} uses right-closed size quantiles; \code{rank} uses
approximately equal-size blocks after stable sorting by size.}

\item{common_mean}{NULL keeps robust fitted means; TRUE substitutes the
ordinary mean of finite scores among providers selected for fitting; a
number supplies a common mean. Group scales are not refitted under this
override. Applies only to robust \code{overall} or \code{groupwise} calibration.}

\item{fit_mask}{Logical selection of providers used for robust null fitting.
Excluded providers remain in the result. Structured likelihood fits
currently require all retained providers.}

\item{probabilities}{A list of null Bernoulli probabilities per provider;
a numeric vector is accepted for a single provider.}

\item{eta, re_mean, re_var}{Lists of observation-level predictors and posterior
random-effect means/variances. \code{eta} excludes the target effect;
\code{re_mean} and \code{re_var} default to zero. Deterministic binary tests use
\code{plogis(reference + eta + re_mean)} and require zero \code{re_var}.
Resampling draws independent normal effects per observation with the
supplied means and variances, then simulates Bernoulli outcomes.}

\item{alpha}{Significance level; also defines the confidence level \code{1-alpha}.}

\item{alternative}{\code{two.sided}, \code{greater}, or \code{less}; \code{two_sided} is an alias.}

\item{ci}{\code{auto}, \code{none}, \code{wald}, \code{poisson}, or \code{invert}. Auto supplies Wald
intervals for estimate/SE input, classical Poisson intervals for an
uncalibrated exact Poisson test, and test-inversion intervals for other
Poisson or deterministic binary tests. Scores/p-values alone and simulated
tests do not imply an original-scale confidence interval.}

\item{mid_p}{Use mid-p two-sided Poisson-binomial or resampling tails.
One-sided binary tails are inclusive unless
\code{control$one_sided_midp=TRUE}. The named Poisson statistics determine their
own tail convention independently of this argument.}

\item{zero_method}{For zero log-ratios, use the \code{score} approximation
(\code{-1/se}, requiring a log-scale SE and unit reference) or \code{exclude}.
Score intervals at zero are approximations marked in the diagnostics.}

\item{id}{Optional unique provider identifiers.}

\item{na_action}{\code{fail} rejects missing inputs; \code{omit} omits incomplete
providers from computation but preserves their rows with missing results.}

\item{control}{Named list of numerical and estimation controls; see Details.}
}
\value{
An object of class \code{fit_pci} containing:
\describe{
\item{results}{One row per input provider, with raw and adjusted scores
and p-values, null means/SDs, directions, interval endpoints and scale,
and indicators \code{included} and \code{used_for_null}. \code{as.data.frame()} returns
this table. \code{p_raw} and \code{p_value} use the requested alternative.}
\item{null_fit, null_parameters}{Estimator output and the parameters used
for calibration. Specified theoretical/fixed nulls have no fitted model.}
\item{diagnostics}{Status, warning codes, omitted identifiers, and
calibration/interval diagnostics. Inspect these before interpretation.}
\item{method, statistic_details}{Selected methods, numerical settings,
and statistic-specific input/calculation details.}
\item{call, package_version}{Call and package-version metadata.}
}
}
\description{
Construct a provider statistic, apply a specified null distribution, and
return provider-level p-values, directional flags, confidence intervals, and
diagnostics through one interface. Supply estimates and their
standard errors, observed/expected counts, null probabilities, or scores
computed from a fitted model. The function does not fit the outcome model.
}
\details{
\subsection{Statistic and null distribution}{

Choose \code{statistic} for the input and initial test, then \code{calibration} for
the score distribution. Under fixed or estimated normal nulls the adjusted
score is \eqn{(z_i-\mu_i)/\sigma_i}; its two-sided p-value is
\eqn{2\Phi(-|(z_i-\mu_i)/\sigma_i|)}. The theoretical mode preserves raw
tail probabilities, including Student-t and discrete tails. Thus theoretical
calibration and fixed \verb{null_mean=0, null_sd=1} need not agree for discrete
tests whose score conversion reaches a numerical floor.

For a count \eqn{O} under its null distribution, define the one-sided
mid-p tails \eqn{L=P(X<O)+P(X=O)/2} and
\eqn{U=P(X>O)+P(X=O)/2}. The two-sided mid-p is
\eqn{2\min(L,U)}. Equivalently, with \eqn{q_l=2L} and \eqn{q_u=2U},
it is \eqn{\min(q_l,q_u)}, without a further division by two.
Mid-p is a half-mass convention and need not provide conservative exact
test size. For \code{poisson_exact}, the two-sided convention instead doubles
the inclusive upper tail if \code{observed > expected * reference}, or the
inclusive lower tail otherwise, and caps the result at 0.999. It is not
a probability-ordering definition of a two-sided Poisson test.

\code{overall} defaults to robust location/scale estimation. \code{groupwise} fits
that estimator separately by supplied groups or size groups, using
\code{\link[MASS:rlm]{MASS::rlm()}} with MAD scale and the selected influence function.
\code{control=list(estimator="likelihood")} selects the overall likelihood
estimator. The structured \code{individualized}, \code{correlated}, \code{cre}, and
\code{moment} models use the estimators exposed by \code{\link[=fit_empirical_null]{fit_empirical_null()}}.
Moment calibration requires Poisson Pearson scores, unit reference, and
\code{second_moment}; for supplied scores, set \code{control$score_type="pearson"}.
Exact moment expressions do not make the working Gaussian tails exact
Poisson tests.
}

\subsection{Numerical and estimation controls}{

Robust controls are \code{estimator}, \code{maxit} (1000), \code{acc} (1e-8), \code{tuning}
(1.345 for Huber, 4.685 for bisquare), \code{min_group_size} (3), and
\code{small_group} (\code{"error"}, or \code{"theoretical"} for a recorded fallback).
Likelihood controls are \code{p_grid}, \code{phi_bounds}, \code{cutoff},
\code{include_intercept}, and individualized \code{common_intercept}, as applicable
to the selected model. Inapplicable calibration controls are rejected.

Statistics controls are \code{p_floor} (one-sided normal-score floor, 1e-6),
\code{seed} (1), \code{n_resample} (10000), and \code{one_sided_midp} (FALSE).
Resampling preserves the caller's random-number state. Its tail proportions
are Monte Carlo estimates and may be zero; they are not exact probabilities.
Interval controls are \code{normal_threshold} (expected-count threshold for
Byar intervals, 100), \code{upper_cap} (Poisson root-search limit, 1e8),
\code{root_tol} (1e-8), and \code{max_log_odds} (binary inversion limit, 40).
}

\subsection{Intervals and interpretation}{

Estimated/fixed normal nulls use signed normal scores; extreme discrete
tails can be limited by the chosen score floor. Normal-score
null parameters are held fixed during interval inversion; uncertainty in
estimated null parameters is not propagated. Binary inversion intervals
are on the log-odds-shift scale relative to supplied null probabilities,
not automatically on a standardized provider rate or ratio scale.
Numerical inversion failures, empty sets, and detected nonmonotone binary
score curves are reported in diagnostics and do not produce fabricated
interval endpoints. The binary monotonicity check is a numerical guard,
not a proof for every probability vector.

Inference directions are -1 (lower), 0 (not flagged), +1 (higher), without
assigning clinical desirability. A supplied covariance/posterior SE may
come from a robust model fit; this function does not recompute covariance
matrices or refit models for likelihood-ratio tests. Such model-derived
test p-values can enter through \code{statistic="p_value"} with signed direction.
Converting such a supplied two-sided p-value to one-sided inference assumes
the symmetric-tail convention encoded by its direction. No multiplicity
adjustment is applied across providers.
}
}
\examples{
# A specified score-distribution SD; this does not set an outcome benchmark.
fit_pci(z = c(-3, 0, 3), calibration = "fixed", null_sd = 1.81)

# Poisson mid-p inference for observed/expected ratios.
fit_pci(observed = c(0, 13), expected = c(2, 7.385),
        statistic = "poisson_midp")

# Test proportions against a 30\% benchmark using original-scale SEs.
fit_pci(estimate = c(0.2, 0.4), se = c(0.04, 0.06),
        statistic = "wald", transform = "logit", reference = 0.3)

# Group-specific robust location and scale from provider scores.
set.seed(17)
grouped <- fit_pci(z = rnorm(80, 0.1, 1.2), size = seq_len(80),
                   calibration = "groupwise", n_groups = 4, psi = "huber")
head(as.data.frame(grouped))
grouped$diagnostics$warning_codes
}
\seealso{
\code{\link[=fit_empirical_null]{fit_empirical_null()}}, \code{\link[=empirical_null_groupwise]{empirical_null_groupwise()}},
\code{vignette("fit_pci", package = "EmpiNull")}
}
